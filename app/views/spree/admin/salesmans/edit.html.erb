<% content_for :page_title do %>
  <%= link_to Spree.t(:salesman), spree.admin_salesmans_url %>
  <%= Spree.t(:new_salesman) %>
<% end %>

<div data-hook="admin_salesman_new_form_header">
  <%= render partial: 'spree/admin/shared/error_messages', locals: { target: @user } %>
</div>

<div data-hook="admin_salesman_new_form">
  <%= form_for [:admin, @salesman] do |f| %>
    <%= render partial: 'form', locals: { f: f } %>

    <div data-hook="admin_salesman_edit_form_buttons">
      <div class="form-actions" data-hook="buttons">
        <%= button Spree.t('actions.update'), 'refresh', 'submit', {class: 'btn-success', data: { disable_with: "#{ Spree.t(:saving) }..." }} %>
        <span class="or"><%= Spree.t(:or) %></span>
        <%= button_link_to Spree.t('actions.cancel'), admin_salesmans_url, icon: 'delete' %>
      </div>
    </div>
  <% end %>
</div>